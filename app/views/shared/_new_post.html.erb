<%= form_for [user, user.posts.build], remote: true do |f| %>
  <%= f.text_area :body, placeholder: "What's up?" , class: 'form-control' %>
  <%= f.submit 'post', class: 'btn btn-primary' %>
<% end %>